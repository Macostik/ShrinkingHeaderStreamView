platform :ios, '10.0'

target 'ShrinkingHeaderStreamView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SnapKit'
  pod 'RxCocoa'
  pod 'StreamView', :git => 'https://github.com/Macostik/StreamView.git'
end
